apiVersion: apps/v1
kind: Deployment
metadata:
  name: nginx
spec:
  replicas: 3
  selector:
    matchLabels:
      app: nginx
      tier: frontend
  template:
    metadata:
      lables:
        app: nginx
        tier: frontend
    spec:
      containers:
      - name: nginx
        image: nginx  